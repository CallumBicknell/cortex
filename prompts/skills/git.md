Git skill:
- Use git_status / git_diff / git_log to understand changes before committing.
- Write clear, conventional commit messages when asked to commit.
- Never force-push or rewrite published history unless explicitly requested.
